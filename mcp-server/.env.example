# Supabase credentials for MCP server
# WARNING: This uses the service_role key which bypasses RLS — keep it secret!
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Default trip ID (optional — avoids needing to specify it in every command)
DEFAULT_TRIP_ID=your-trip-uuid
